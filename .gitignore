# build output
dist/
# generated types
.astro/
# generated by scripts/generate-redirects.mjs from src/data/affiliate-links.json
public/_redirects

# dependencies
node_modules/

# pnpm local content-addressable store (created by pnpm install in the repo
# directory — should never be committed; CI installs into its own store)
.pnpm-store/

# npm lockfile — pnpm is the workspace package manager
package-lock.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo


# environment variables
.env
.env.production
.env.local

# local credential scratchpad — NEVER commit
keys.md
secrets.md

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
